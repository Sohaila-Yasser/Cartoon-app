/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.rollingstone.com", "s.tmimgcdn.com", "i.postimg.cc", "adsbalance.com"],
  },
};

module.exports = nextConfig;

